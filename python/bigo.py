colors = ['green', 'yellow', 'blue', 'pink']


# O(n) complexity
def linear(colors):
    # Iterate the elements of the list
    for color in colors:
        # Print the current element of the list
        print(color)


linear(colors)
